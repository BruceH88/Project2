<div class=" text-center my-3 py-2 px-2 border border-secondary content-post rounded">
  <p class="text-right font-italic mb-1">{{createdAt}}</p>

  <img src="{{imgSrc}}" alt="{{imgAlt}}" width="90%" class="">

  <p class="text-left py-2">{{text}}</p>

  <p class="text-right font-italic">-posted by <span>{{author}}</span> in <a href="/topics/{{topic}}">{{topic}}</a></p>

</div>


{{!-- <li>
  {{name}}

  <button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
    {{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
  </button>

  <button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li> --}}